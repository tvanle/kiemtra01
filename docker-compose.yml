version: '3.8'

services:
  db_customer:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: customer_db
    ports:
      - "3331:3306"

  db_staff:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: staff_db
    ports:
      - "3333:3306"

  db_laptop:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: laptop_db
    ports:
      - "5431:5432"

  db_clothes:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: clothes_db
    ports:
      - "5435:5432"

  db_cart:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: cart_db
    ports:
      - "5437:5432"

  api_gateway:
    build:
      context: ./api_gateway_service
    ports:
      - "8031:80"
    depends_on:
      - customer_service
      - staff_service
      - laptop_service
      - clothes_service
      - cart_service

  customer_service:
    build:
      context: ./customer_service
    ports:
      - "8033:8000"
    environment:
      - DATABASE_URL=mysql://root:rootpassword@db_customer:3306/customer_db
    depends_on:
      - db_customer

  staff_service:
    build:
      context: ./staff_service
    ports:
      - "8035:8000"
    environment:
      - DATABASE_URL=mysql://root:rootpassword@db_staff:3306/staff_db
    depends_on:
      - db_staff

  laptop_service:
    build:
      context: ./laptop_service
    ports:
      - "8037:8000"
    environment:
      - DATABASE_URL=postgres://user:password@db_laptop:5432/laptop_db
    depends_on:
      - db_laptop

  clothes_service:
    build:
      context: ./clothes_service
    ports:
      - "8039:8000"
    environment:
      - DATABASE_URL=postgres://user:password@db_clothes:5432/clothes_db
    depends_on:
      - db_clothes

  cart_service:
    build:
      context: ./cart_service
    ports:
      - "8041:8000"
    environment:
      - DATABASE_URL=postgres://user:password@db_cart:5432/cart_db
    depends_on:
      - db_cart
